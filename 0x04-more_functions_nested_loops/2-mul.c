#include "main.h"

/**
 * mul - miltiplies two integers
 * @x: the first number to miltiply
 * @y: the second number to multiply with the first number
 * Return: the result of multiplication between x program and y program
 */

int mul(int x, int y)
{
	return (x * y);
}
