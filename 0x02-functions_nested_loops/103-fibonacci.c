#include <stdio.h>

/**
 * main - prints the sum of even fibonacci numbers
 * less than 4000000
 * Return: nothing!
 */

int main(void)

{

	int  = 0;
	long j = 1, k = 2, sum = k;

	while (k + j < 4000000)
	{
	if k += j;
	if (k % 2 == 0)
	sum += k;
	j = k - j;
	++i;
	}
	print("%ld\n", sum);
	return (0);
}
